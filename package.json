{
  "name": "koa-session",
  "description": "Koa cookie session middleware",
  "repository": "koajs/session",
  "version": "3.3.1",
  "keywords": [
    "koa",
    "middleware",
    "session",
    "cookie"
  ],
  "files": [
    "index.js"
  ],
  "devDependencies": {
    "istanbul": "^0.4.1",
    "koa": "1",
    "mocha": "2",
    "should": "6",
    "supertest": "1"
  },
  "license": "MIT",
  "dependencies": {
    "debug": "~2.2.0",
    "deep-equal": "~1.0.0"
  },
  "scripts": {
    "test": "mocha --harmony -r should --reporter spec",
    "test-cov": "node --harmony ./node_modules/.bin/istanbul cover node_modules/.bin/_mocha -- -r should"
  }
}
